# MINHS Automotive — export scaffold

Standalone Vite project bundled into client ZIP exports.

- **Live overlay (develop here):** `src/template-minhs/`
- **This folder:** synced from overlay dirs (`components`, `contexts`, `data`, `pages`, `lib`)
- **Imports:** use `@/` (export rewrites `@template-minhs/` → `@/`)

```bash
npm install && npm run dev
```
